import styled from 'styled-components'

const Page = styled.div`
    width: calc(100% - 300px);
    position: absolute;
    right: 0;
    height: 100vh;
    margin: auto;
    display: flex;
    justify-content: center;
`

export { Page }
